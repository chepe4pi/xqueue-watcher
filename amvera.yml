meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
run:
  command: python -m xqueue_watcher -d conf.d/
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
